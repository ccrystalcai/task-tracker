# 开发日志 — 2026-05-27

## 今日完成

- [x] 项目需求整理与确认
- [x] 技术选型方案确定
- [x] 创建 docs/ 文件夹，编写需求、技术、设计、开发计划四份标准文件
- [x] 创建 devlog/ 开发日志文件夹
- [x] 创建 CLAUDE.md 项目 AI 指引文件
- [x] **Phase 1 完成：项目骨架搭建**
  - Vite + React 18 + TypeScript 项目初始化
  - Tailwind CSS 3 配置，5 套主题 CSS 变量（Daylight / Midnight / Forest / Sunset / Ocean）
  - React Router 6 路由配置，6 个页面占位
  - Dexie.js 数据库 Schema 定义，5 张表（goals, tasks, taskRecords, journalEntries, dailySummaries）
  - 布局组件：Sidebar 导航 + Header + AppLayout
  - 主题切换：Zustand store + useTheme hook + Settings 页面主题选择器
  - TypeScript 编译零错误，生产构建成功（306KB JS + 13KB CSS）

## 待办事项

- [x] Phase 6：反思日记页
  - journalStore (Zustand)：entries 状态管理，upsertEntry 自动判断创建/更新
  - Journal 页面：按月分组展示日记卡片（心情 emoji + 日期 + 内容摘要 + 建议数量）
  - 展开查看完整日记：心情、内容、优化建议列表
  - 编辑弹窗：修改心情和内容
  - 创建日记：手动写新日记（选择日期、心情、内容）
  - 删除日记（带确认）
  - 空状态引导页
- [x] Phase 7：全局搜索
  - Fuse.js 模糊搜索（threshold 0.4）
  - 同时搜索任务（标题、描述、反思、备注）和目标（名称、描述）
  - 搜索防抖 200ms
  - 关键词高亮（匹配文字黄色标记）
  - 任务结果：状态图标 + 优先级标签 + 耗时 + 日期 + 评分 + 标签徽章
  - 目标结果：目标色块 + 任务进度 + 状态标签
  - 输入框自动聚焦
- [x] Phase 8：Google Calendar 集成
  - src/utils/calendar.ts：动态加载 Google Identity Services 脚本
  - OAuth 2.0 隐式授权流程（TokenClient），scope: calendar.events
  - Token 存储在 localStorage，自动检查过期
  - Calendar API 封装：获取日历列表、创建活动、列出活动
  - Settings 页面新增 Google Calendar 区块：
    - Client ID 输入和保存
    - 连接/断开按钮
    - 日历选择器（连接后自动拉取）
    - 同步未来任务到日历（最多 20 个，支持 dueTime 转 datetime）
    - 成功/错误提示
- [ ] 后续可优化：Google Calendar 事件导入为任务

## 待办事项

- [ ] Phase 3：今日看板增强（鼓励弹窗、浏览器通知、完成一天按钮）

## 遇到的问题

- npm create vite 在非空目录下被取消，改为手动创建所有配置文件
- Dexie `transaction()` 超过 5 个表需要用数组传参

## 备注

- 用户确认需求：纯网页、本地存储、Google 日历、手动拆解 + 模板
- 开发服务器运行在 http://localhost:5173
- **四象限+分析统计升级**：
  - Dashboard 四象限：每个象限显示任务数、预估耗时、完成进度条、时间占比百分比
  - 今日时间分配：视觉化横向条形图展示各象限时间占比
  - 智能洞察：根据四象限分布自动给出效率分析（如"救火模式"/"良好状态"/"干扰过多"）
  - Analytics 标签筛选：点击标签筛选任务，支持多选
  - Analytics 统计卡片：总任务/完成率/预估耗时/效率指数
  - 本月热力图：类似 GitHub 贡献图，悬停显示详情
  - 标签维度时间分布：按标签查看时间投入
  - 效率分析面板：四个指标 + 优化建议
- **需求变更**（数据模型调整）：
  - 任务与目标解耦：任务可为独立任务（goalId 可为 null），目标一定由任务组成
  - 单任务提醒：每个任务可独立设置提醒时间（reminderEnabled + reminderTime）
  - 标签系统：新增 Tag 接口 + tags 表，任务支持打多标签（tags: string[]）
- **计时器 + 周期任务**：
  - 新增 FocusSession 表，记录每次专注的起止时间和累计时长
  - useTimer hook：播放/暂停/累计计时，自动计算总秒数和专注次数
  - TimerTaskItem 组件：统一 Dashboard 和 Goals 页的任务行，集成计时器
  - 每个任务支持多次"开始→暂停→继续"循环，显示 N 次专注和累计耗时
  - 预估 vs 实际耗时对比（实际超时变橙、按时完成变绿）
  - 任务创建支持周期设置：不重复/每天/每周/每月 + 间隔天数 + 结束日期
  - 周期任务在任务行显示 🔄 标记
- **完成一天 + 打分反思 + 日记**：
  - Task 新增 score (1-5星)、reflection (反思)、notes (备注) 字段
  - DaySummaryModal：点击「完成一天」按钮弹出小结窗口
  - 小结自动汇总：完成任务数、实际耗时、剩余未完成
  - 逐任务打分：⭐1-5 星评分
  - 逐任务反思：文本输入框
  - 心情选择：5 档 emoji（😄😊😐😔😫）
  - 今日总结：自由文本输入
  - 自动生成优化建议：根据心情+完成率+象限分布
  - 同时保存 DailySummary + JournalEntry
  - Settings 可设置每日固定提醒时间
- **Phase 3 完成：侧边栏折叠 + 鼓励系统 + 浏览器通知**：
  - Sidebar 折叠：点击底部「收起菜单」→ 60px 纯图标模式，点击右箭头展开
  - AppLayout 同步调整内容区 margin，transition-all 平滑动画
  - 鼓励弹窗：全部任务完成 → 底部弹出庆祝 toast（🎉 随机表扬语）
  - 连续打卡里程碑：3/7/14/21/30/60/100 天自动弹出祝贺
  - 浏览器通知：请求 Notification 权限，每分钟检查任务提醒时间并推送
  - 每日小结提醒：Settings 设置固定时间，浏览器到点通知
